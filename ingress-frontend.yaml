apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-external
  namespace: projeto
spec:
  ingressClassName: nginx
  rules:
  - host: loja-online.35-237-38-104.nip.io
    http:
      paths:
      - backend:
          service:
            name: frontend-external
            port:
              number: 80
        path: /
        pathType: Prefix
status:
  loadBalancer: {}
